apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-service.name" . }}
spec:
  selector:
    app: {{ include "infra-service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.readinessProbe.httpGet.port }}
      targetPort: {{ .Values.readinessProbe.httpGet.port }}
