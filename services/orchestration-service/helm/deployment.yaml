apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestration-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orchestration-service
  template:
    metadata:
      labels:
        app: orchestration-service
    spec:
      containers:
        - name: orchestration-service
          image: registry.example.com/orchestration-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: KAFKA_BOOTSTRAP
              value: "{{ .Values.kafka.bootstrap }}"
---
apiVersion: v1
kind: Service
metadata:
  name: orchestration-service
spec:
  selector:
    app: orchestration-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
