{
  "name": "billing-service",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "mvn package" }, "outputs": ["target"] },
    "test": { "executor": "nx:run-commands", "options": { "command": "mvn test" } },
    "serve": { "executor": "nx:run-commands", "options": { "command": "mvn spring-boot:run" } }
  }
}
