apiVersion: v1
kind: Service
metadata:
  name: {{ include "workspace-service.name" . }}
spec:
  selector:
    app: {{ include "workspace-service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.readinessProbe.httpGet.port }}
      targetPort: {{ .Values.readinessProbe.httpGet.port }}
