apiVersion: apps/v1
kind: Deployment
metadata:
  name: configuration-service
  namespace: aic-platform
  labels:
    app: configuration-service
    tier: core
    version: v1.0.0
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: configuration-service
  template:
    metadata:
      labels:
        app: configuration-service
        tier: core
        version: v1.0.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: configuration-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
      - name: configuration-service
        image: 002aic/configuration-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8080"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: config-db-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: redis-secret
              key: url
        - name: ENVIRONMENT
          value: "production"
        - name: LOG_LEVEL
          value: "info"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
      volumes:
      - name: config
        configMap:
          name: configuration-service-config
---
apiVersion: v1
kind: Service
metadata:
  name: configuration-service
  namespace: aic-platform
  labels:
    app: configuration-service
    tier: core
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: configuration-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: configuration-service
  namespace: aic-platform
  labels:
    app: configuration-service
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: configuration-service-config
  namespace: aic-platform
data:
  config.yaml: |
    server:
      port: 8080
      timeout: 30s
    cache:
      ttl: 300s
      max_size: 1000
    validation:
      strict_mode: true
    logging:
      level: info
      format: json
